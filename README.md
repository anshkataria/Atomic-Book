# Atomic Book

A booking system that ensures **atomic transactions** — meaning a ticket is either booked completely or not booked at all.

#### Package Structure

atomic_book/
            entitiy/



Entity: These are Java Classes that represent the database tables.
            
